/**
 * Status: KERANGKA (implementasi penuh menyusul).
 * Proyek — grid kartu proyek lengkap.
 */
export default function Projects() {
  return (
    <section className="mx-auto w-full max-w-6xl px-4 py-12 md:px-8">
      <p className="text-xs font-semibold uppercase tracking-[0.2em] text-accent">Portofolio</p>
      <h1 className="mt-2 text-3xl font-bold">Proyek</h1>
      <p className="mt-4 rounded-2xl border border-line bg-surface p-6 text-muted">
        Kerangka halaman — daftar proyek lengkap menyusul.
      </p>
    </section>
  )
}
